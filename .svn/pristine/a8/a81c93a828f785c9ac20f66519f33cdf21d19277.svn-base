import React from 'react';


import Dialog, { withLazy } from '@/components/Dialog';

function MessageIndex(props) {
  const { title, values: {data={}, message}, onClose} = props;
  const close = () => {
    console.log('挂')
    onClose()
  }
  const text = {'lineHeight': '20px'}
  return (
    <Dialog title={title} {...props} onCancel={close} footer={null}>
      {console.log(props)}
  <div style={text}>{`上传状态：${message}`}</div>
  <div style={text}>{data.summary}</div>
  {/* <div style={text}>上传考生名单：{data.failList[0].personInfoList}</div> */}
  <div style={text}>建模状态： {!!data.failList.length? data.failList[0].description: '无'}</div>
    </Dialog>
  );
}

MessageIndex.defaultProps = {
  width: '600px',
  title: '上传日志',
};

export default withLazy(MessageIndex)
